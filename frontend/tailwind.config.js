/**
 * tailwind.config.js — kept for tooling/IDE support reference.
 *
 * In Tailwind CSS v4 the theme is configured directly in CSS via @theme
 * inside src/styles/index.css. This file is no longer used at runtime.
 *
 * See: https://tailwindcss.com/docs/v4-upgrade
 */
export default {};
